// Built-in types: number, string, boolean, null, undefined, object
// Typescript specific: any, unknown, never, enum, tuple

//Tuples are useful only in pair of 2
let user: [number, string] = [1, 'Stefan']
user[0].toString
user[1].charAt